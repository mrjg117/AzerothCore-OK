# 2026-08-29

## CI: commit 91ec66e（ccache 改用 buildkit-cache-dance）run 审查

- 审查目标 run = 33218675109（build-static-verify，91ec66e3，success，1h5m17s）。
- 重要纠偏：该 run 来自 `build-static-verify.yml`，**不含 buildkit-cache-dance step**（仅 actions/cache@v4）。真正的 cache-dance 在 `build-core.yml`，触发方式是 `on: workflow_dispatch:`（仅手动），**仓库历史仅 2 次 run 全是 build-static-verify，build-core.yml 从未运行**。
- 因此“cache-dance 是否生效”无法从该 run 判定——它从未被跑过；日志里 200B 空缓存是 build-static-verify 的 actions/cache，非 cache-dance 失败证据。
- 功能验证 gate 通过：acore_playerbots=30 表、auth=23/characters=127/world=328；部署无错误；无致命/构建错误。
- 结论交付：cache-dance 效果“未验证/从未运行”；建议手动 dispatch build-core.yml 验证，或走备选 A（CI 宿主 cmake+ccache），并建议把 cache-dance 也搬进 build-static-verify 使其 on-push 生效。
- 日志证据已落盘：workspace 根 `cache_dance_run.log`（约 1.75MB / 8087 行）。
